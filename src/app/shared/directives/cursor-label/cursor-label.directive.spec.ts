import { CursorLabelDirective } from './cursor-label.directive';

describe('CursorLabelDirective', () => {
  it('should create an instance', () => {
    const directive = new CursorLabelDirective();
    expect(directive).toBeTruthy();
  });
});
